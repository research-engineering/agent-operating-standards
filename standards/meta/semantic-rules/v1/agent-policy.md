# Agent Policy: Semantic Rules v1

Agents MUST apply semantic rules only for the standard that names them.

Agents MUST report semantic failures by rule id and affected artifact field.

Agents MUST NOT turn personal writing preference into semantic rules unless the
rule protects an explicit invariant.
